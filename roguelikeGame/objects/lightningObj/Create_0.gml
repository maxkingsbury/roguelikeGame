// Initialize chain lightning variables
target = noone; // The enemy the lightning will jump to
timer = 0; // Time to track the duration of each lightning connection
max_duration = 180; // Duration the lightning stays visible (10 frames)
alpha = 1; // Full opacity initially
fade_speed = 0.1; // Speed at which the lightning fades out
hit = false