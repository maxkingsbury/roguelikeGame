// Enemy properties
hp = 2;  // Each enemy starts with 5 health
max_hp = 2;  // Maximum health (use this to calculate health percentage)
alive = true; // Tracks if the enemy is still alive
move_speed = 3; // Adjust as needed
slow_count = 0;

image_speed = 0;
image_yscale = 3;